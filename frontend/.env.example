# Backend API base URL. In dev, Vite proxies /api to localhost:8000 (see
# vite.config.js) so this can stay empty. In production (Render), set this
# to the backend's public URL, e.g. https://elcoraldb.alz.name.ng
VITE_API_URL=
